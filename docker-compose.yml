version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - JWT_SECRET=luxecommerce_production_secret_key_2026
    volumes:
      - sqlite_data:/app/backend/data

volumes:
  sqlite_data:
