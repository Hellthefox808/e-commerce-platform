# LuxeCommerce Frontend Environment Configuration
# In development (when using Vite proxy), leave blank to proxy /api to http://localhost:5000
# In production, specify the backend REST API base URL:
VITE_API_URL=http://localhost:5000
